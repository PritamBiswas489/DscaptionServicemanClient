import {View, Text} from 'react-native';
import React from 'react';

export const Cart = ({openCartModal}: any) => {
  return (
    <View style={{flex: 1, justifyContent: 'center', alignItems: 'center'}}>
      <Text>Cart Screen</Text>
    </View>
  );
};
